# Sessione 2026-08-11 — Creazione CLAUDE.md

## Obiettivo

Analisi del repository e creazione di un file `CLAUDE.md` che documenti
la struttura del progetto, le convenzioni e il workflow per gli AI assistant.

## Cosa è stato fatto

- Analizzata la struttura del repository (cartelle, file esistenti, `.gitignore`)
- Letti `README.md`, `docs/hardware.md.`, i due `secrets.yaml.example`
- Creato `CLAUDE.md` nella root con:
  - Descrizione del progetto (Home Assistant + ESPHome + Frigate)
  - Mappa della struttura directory e cartelle pianificate
  - Regola fondamentale sui segreti (mai versionare credenziali)
  - Convenzioni per lingua, YAML, ESPHome, Home Assistant, Frigate
  - Vincoli hardware per il lettore microchip (134,2 kHz FDX-B)
  - Tabella di stato dei componenti ancora da implementare
  - Hardware ancora da definire
- Commit e push sul branch `claude/claude-md-docs-t680dv`

## Decisioni prese

| Argomento | Decisione |
|---|---|
| Lingua documentazione | Italiano |
| Segreti | Solo `!secret` in YAML; mai valori inline |
| Organizzazione HA | Package per area/funzione in `packages/` |
| Integrazione ESPHome→HA | API nativa (non MQTT salvo necessità) |
| Riconoscimento facciale | Solo Frigate nativo (>= 0.16), no terze parti |
| Lettore microchip | 134,2 kHz FDX-B obbligatorio; RC522 e RDM6300 esclusi |

## Stato attuale del repository

Il repository è ancora in fase iniziale. I file versionati sono:

```
.gitignore
README.md
CLAUDE.md                         (creato in questa sessione)
docs/hardware.md.
esphome/secrets.yaml.example
homeassistant/secrets.yaml.example
docs/sessioni/2026-08-11-claude-md.md   (questo file)
```

Tutto il resto (firmware ESPHome, configurazione HA, Frigate, package)
è ancora da implementare.
